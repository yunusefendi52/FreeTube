workflows:
  my-workflow:
    environment:
      node: 16.9.0
      npm: latest
    
    scripts:
      - name: Install and build
        script: |
          npm install -g yarn
          yarn install
          yarn run build:arm64