workflows:
  my-workflow:
    environment:
      node: 16.10.0
    
    scripts:
      - name: Install and build
        script: |
          npm install -g yarn
          yarn install
          yarn run build:arm64